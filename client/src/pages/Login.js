//import {} for './../components/Styles';
import { Formik, Form } from 'formik;'

const Login = () => {
  return (
    <div>
       <h2>Please LogIn</h2>
       <Formik>
          {() = > (
            <Form>

            </Form>
          )}
       </Formik>
    </div>
  )
};

export default Login;
